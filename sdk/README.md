# Plugin SDKs

SDKs will provide generated event types, manifest validation, test fixtures,
and local-host clients. Rust and TypeScript are expected first; the IPC
contract will allow Python, C#, Go, and other languages.

