# Desktop application

The future Windows and Linux desktop host will manage capture, replay, plugins,
overlays, local logs, and website device pairing.

Its interface toolkit has not been selected yet. The engine and plugin APIs do
not depend on that choice.

