# Plug-in examples

These are development examples, not automatically installed packages. Copy an
example's entire directory into `plugins/installed/` to let the host discover
it.

- `bpsr-uid-aliases/` demonstrates a data-only localization transform that
  imports the canonical BPSR catalog and applies user-facing UID aliases after
  normal localization.
