rLogs v0.1.78 repairs raid completion capture and cleans up legacy profile routing data.

- Treats Field of Forgotten Illusions Normal, Hard, and Nightmare scenes as raids, using the exact game-file boss identities and packet-observed boss objectives.
- Keeps one Gauntlet run open when the next boss objective arrives, while sealing a selected-boss Nightmare run only after its completed boss objective is followed by the run exit or scene departure.
- Emits an authoritative completion boundary from the packet-proven final boss objective so completed raids can be sealed, recovered, and submitted.
- Uses raid-specific segments for raid boss damage and stops the encounter clock on packet-proven boss completion.
- Keeps one claimed profile per stable character UID when packet routing refines a legacy `global` region to `north-america`.
- Adds a server-authorized website developer role and a per-browser developer-mode preference for unfinished site surfaces.
- Adds recoverable operator migrations for deduplicating profile regions and clearing invalid public parse projections while preserving source artifacts and backups.
- Adds a focused event-slice filter for investigating high-volume packet captures without loading unrelated events.
- Updates the Blue Protocol: Star Resonance game plug-in to v0.1.10.
