# rLogs v0.1.125

## Shared packet-verified dummy filtering

- Move the desktop dummy validator into the shared BPSR rules package so the upcoming hosted evidence replay can use the exact same implementation instead of duplicating leaderboard rules.
- Count only the locked player's direct or owned-Imagine damage against the exact locked Guild Hall dummy; unrelated sources and targets cannot enter the result.
- Preserve pre-existing packet-observed status effects when dummy mode is armed and invalidate external player support even when its ownership arrives later.
