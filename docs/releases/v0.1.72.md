# rLogs v0.1.72

- Keeps the localized dungeon scene name in the overlay header while boss identity and health remain in the dedicated boss summary row.
- Freezes the visible run timer at the packet-observed death of the reviewed dungeon boss instead of waiting for the later settlement packet.
- Retains final mobbing damage through the post-kill transition window and creates the retry baseline only when Boss-tier combat actually begins.
- Prevents the global inactivity presentation option from subtracting time from encounter and run clocks that a packet boundary already froze.
- Preserves game-owned boss, dungeon-finish, and continuous-encounter configuration when the live reducer starts a new capture.
