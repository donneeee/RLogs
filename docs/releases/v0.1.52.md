rLogs v0.1.52

- Makes the Combat Overlay timer a truly fixed-width control across saved settings, the designer preview, and the native live window.
- Normalizes existing timer widths automatically, so older layouts no longer shift the scene title or neighboring controls when the clock label changes.
- Removes timer resizing from the designer while retaining user-controlled widths for every other summary control.
