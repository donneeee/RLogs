rLogs v0.1.107 advances the screen-sized overlay canvas with an exact, packet-backed local-player HUD module.

- Adds a movable and resizable player frame beside the existing Mechanics Map and current-target modules.
- Shows the local character's observed name, current and maximum HP, shield totals, and live/dead/stale state without borrowing profile values.
- Preserves independent player-frame position and width across launches, while clamping restored modules to the current display resolution.
- Advances the bounded Mechanics Map contract to schema 5 so player vitals and the existing target/map state arrive in one coherent revision.
- Keeps the canvas at the user's full viewport resolution and retains unrestricted map zoom and pan.
