rLogs v0.1.87 restores packet-observed cast counts across live and published skill breakdowns.

- Joins outgoing `UseSkill` cast IDs to their exact current-build Recount damage families, so casts remain distinct from landed hits and missed ranged attacks are represented correctly.
- Generates 4,002 fail-closed relations from the current `SkillTable`, `SkillEffectTable`, `SkillFightLevelTable`, and `RecountTable`; 15 ambiguous skill routes remain deliberately unclaimed rather than guessed.
- Enriches canonical server replay and cross-vantage reconciliation before publishing schema-12 reports, allowing existing stored artifacts to rebuild with cast-family evidence instead of requiring a new capture.
- Applies the same localized action identity and Recount family to compact live-overlay rows, including raw child damage IDs used by every class and specialization.
- Keeps cast evidence honest: remote perspectives without outgoing cast packets continue to say `Casts not observed`, and casts are never inferred from hit totals.
- Keeps the website's complete `Other` skill dialog available for compacted rows and validates schema-12 reports and reconciliations.
- Updates the bundled Blue Protocol: Star Resonance game plug-in to v0.1.16 and Combat Overlay plug-in to v0.1.14.
