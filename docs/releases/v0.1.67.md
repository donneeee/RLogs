# rLogs v0.1.67

- Makes the Combat Overlay designer open with stable, populated example combat every time. Live overlay data remains available from the Preview data selector.
- Replaces ambiguous summary checkboxes with explicit “Show …” labels, disables width inputs for hidden items, and gives Remove controls a clearly visible destructive style.
- Corrects the Account & Profiles connection form spacing and alignment at desktop and narrower window sizes.
- Adds compact combined-Link summaries above the currently equipped modules and every recommended loadout, without power or threshold clutter.
- Updates the Combat Meter, Combat Overlay, and Module Optimizer plug-in versions shown by rLogs.
- Fixes the website’s Other-skills row so it consistently opens the complete grouped-skill dialog, including clicks on nested row content.
