# rLogs v0.1.130

- Detects the numeric client build from the first packet envelope instead of relying on launcher or install flavor.
- Refreshes the reviewed local map pack automatically after capture starts on a new game build.
- Verifies every expected image and catalog before treating a build-scoped cache as complete.
- Serializes automatic and manual refreshes so they cannot replace the cache concurrently.
- Keeps new-build mechanic guidance fail-closed until that build has been reviewed.
