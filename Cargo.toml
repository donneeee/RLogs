[workspace]
members = [
    "engine/capture",
    "engine/events",
    "engine/plugin-api",
    "engine/protocol",
    "engine/submission",
    "tools/capture-inspect",
    "tools/protocol-coverage",
]
resolver = "3"

[workspace.package]
edition = "2024"
license = "AGPL-3.0-only"
rust-version = "1.85"
version = "0.1.0"

[workspace.dependencies]
pcap-parser = "0.17.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
