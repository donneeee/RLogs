# Replay fixtures

Only sanitized, deliberately reviewed fixtures may be committed. Every fixture
must record its region, client build, expected decode coverage, expected
canonical events, and privacy review.

The `capture/` fixtures are tiny, synthetic link-layer examples stored as
reviewable hexadecimal text. They contain no real player traffic, identifiers,
addresses, credentials, or game data.
