# Replay fixtures

Sanitized, deterministic canonical-event fixtures live here.

- `reference-combat.source.json` is the editable source.
- `reference-combat.rlog` is the sealed runtime artifact produced by
  `rlogs-rlog-build`.

These files contain no raw packets, credentials, private chat, account data, or
real player identity.
