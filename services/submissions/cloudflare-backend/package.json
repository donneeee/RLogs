{
  "name": "rlogs-cloudflare-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "db:migrate:local": "wrangler d1 migrations apply rlogs-production --local",
    "db:migrate:remote": "wrangler d1 migrations apply rlogs-production --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
