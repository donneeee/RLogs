# Blue Protocol: Star Resonance shared assets

Canonical game icons live in `icons/`. The BPSR game plug-in exports that
folder as the versioned `icons` shared resource for localization, overlays,
profile views, and other plug-ins.
