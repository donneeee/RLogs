# Game-data engine

This crate loads compact, validated end products for constant-time runtime
lookups. It does not locate, inspect, decrypt, or extract game files.

Human-maintained source folders and their validator/compiler live at the
workspace root under `game-data/` and `tools/game-data-build/`.
